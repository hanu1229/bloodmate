import { Box, Typography } from "@mui/joy";

export default function SearchSugarModal(props) {
    return (
        <Box sx = {{padding : "12px", display : "flex", flexDirection : "column", flex : 1, height : "100%"}}>
            <Typography>ㅎㅇ</Typography>
        </Box>
    );
}